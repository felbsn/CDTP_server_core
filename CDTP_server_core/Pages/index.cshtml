@page
@model CDTP_server_core.Pages.indexModel
@{
    Layout = null;

    if(HttpContext.Session.IsAvailable && HttpContext.Session.TryGetValue("user" , out var obj))
    {
        HttpContext.Response.Redirect("/dashboard");
    }else
    {
        HttpContext.Response.Redirect("/login");
    }


}

 